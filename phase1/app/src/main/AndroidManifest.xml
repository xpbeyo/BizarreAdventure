<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.phase1">

    <application
        android:allowBackup="true"
        android:name=".Phase1App"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".SettingActivity"></activity>
        <activity android:name=".WinActivity" />
        <activity android:name=".LoseActivity" />
        <activity android:name=".stage2.TreasureHuntActivity" />
        <activity android:name=".CreatePlayerActivity" />
        <activity android:name=".SelectPlayerActivity" />
        <activity android:name=".ChooseOrCreatePlayerActivity" />
        <activity android:name=".RegisterActivity" />
        <activity android:name=".stage1.g1moveActivity" />
        <activity android:name=".stage3.BattleActivity" />
        <activity android:name=".LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>